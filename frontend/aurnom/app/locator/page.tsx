"use client";

import { LocatorPageChrome, UniversalLocatorMap } from "@/components/universal-locator-map";

export default function LocatorPage() {
  return (
    <LocatorPageChrome>
      <UniversalLocatorMap />
    </LocatorPageChrome>
  );
}
