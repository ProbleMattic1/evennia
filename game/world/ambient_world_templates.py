"""
Ambient template data lives under ``world/data/ambient.d/*.json`` (sorted),
with optional additional rows in ``world/data/ambient_templates.json``.

Load via world.ambient_loader.bootstrap_ambient_registry_at_startup or
load_ambient_from_json.
"""
